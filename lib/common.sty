\ProvidesPackage{common}[2024/02/06]
% The package provides packedges for all other .sty and .tex files.
% It also includes common commands used by other files.

\makeatletter
\@ifundefined{commonstyloaded}{
    \def\commonstyloaded{}

    \RequirePackage{pifont}
    \RequirePackage[dvipsnames, table]{xcolor}
    \RequirePackage{xspace}
    \RequirePackage{bm}
    \RequirePackage{multirow}
    \RequirePackage{newfloat}
    \RequirePackage{caption}
    \RequirePackage{amsmath}
    \RequirePackage[letterspace=150]{microtype}
    \RequirePackage{makecell}
    \RequirePackage[T1]{fontenc}
    \RequirePackage[utf8]{inputenc}
    \RequirePackage{varwidth}
    \RequirePackage{framed}
    \RequirePackage[strict]{changepage}
    \RequirePackage{soul}
    \RequirePackage{luacode}
    \RequirePackage{array}
    \RequirePackage{polyglossia}
    \RequirePackage{enumitem}
    \RequirePackage{graphicx}
    \RequirePackage{parskip}
    \RequirePackage{xstring}
    \RequirePackage{multirow}

    \setstcolor{red}

    \RequirePackage{xspace}
    \xspaceremoveexception{-}
    \xspaceaddexceptions{+}

    \newcommand{\clubs}{\color{OliveGreen}\ding{168}\color{black}\xspace}
    \newcommand{\diams}{\color{BurntOrange}\ding{169}\color{black}\xspace}
    \newcommand{\hearts}{\color{Maroon}\ding{170}\color{black}\xspace}
    \newcommand{\spades}{\color{Blue}\ding{171}\color{black}\xspace}

    \newcommand{\vul}[1]{\color{Maroon}\textbf{#1}\color{black}}
    \newcommand{\nvul}[1]{\color{OliveGreen}\textbf{#1}\color{black}}

    \newcommand{\we}{
        \begin{varwidth}{0.20\linewidth}
            \centering
            \vspace*{-1em}
            W \qquad E
        \end{varwidth}
    }

    \newcommand{\br}{\vspace{0.5em}}
}
\makeatother

